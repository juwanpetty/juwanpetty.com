import path from "path";

export const BLOG_DIRECTORY = path.join(
  process.cwd(),
  "src",
  "content",
  "blog"
);

export const PROJECTS_DIRECTORY = path.join(
  process.cwd(),
  "src",
  "content",
  "projects"
);
